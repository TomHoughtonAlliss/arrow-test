## Backend
Contains some sample code for creating a table using Arrow and Array.

## Frontend
Currently empty, but will be used to demonstrate receiving a ICP-format table and decoding it back into a table, which can then be accessed.
